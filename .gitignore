# Avoid any accidental leakage of our token(s).
*.token.*
*.token

# I dont want to push our generated csv data
*.csv

# I don't want to push txt files I created for miscellanous notes I've produced while working on the project
*.txt

# We don't want to push our IDE configuration folder(s) to our repo.
.idea/
.vscode/
*.sw*


# We don't want to push any precompiled Python files to be pushed either.
module/__pycache__/
module/*.pyc

# Do not modify config.unmodified.json, instead copy it and name it "config.json" so that we don't accidentally push your config to your Github.
config.json